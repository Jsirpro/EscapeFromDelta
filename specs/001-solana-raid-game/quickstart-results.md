# Quickstart Results

Implementation scaffolding was generated for the Escape from Delta demo.

Commands still require dependency installation before they can be executed in
this workspace:

- `anchor build`
- `anchor test`
- `npm run test:client`
- `npm run test:app`
- `npm run query:records -- --wallet <PLAYER_WALLET> --cluster localnet --format table`
